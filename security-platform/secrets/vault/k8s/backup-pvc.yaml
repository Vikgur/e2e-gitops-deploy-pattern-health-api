apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-backup
  namespace: vault
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: standard